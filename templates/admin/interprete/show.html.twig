{% extends 'admin/base.html.twig' %}

{% block title %}Interprete{% endblock %}

{% block body %}
    <div class="card mt-1 m-4 p-3">
        <a href="{{ path('app_admin_interprete_index') }}"> < Retour </a>
        <h1>Interprete</h1>

        <table class="table table-dark">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ interprete.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ interprete.nom }}</td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex">
            <a class="btn btn-success" href="{{ path('app_admin_interprete_edit', {'id': interprete.id}) }}">Modifier</a>
            {{ include('admin/interprete/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
