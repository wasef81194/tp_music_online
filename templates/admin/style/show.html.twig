{% extends 'admin/base.html.twig' %}

{% block title %}Style{% endblock %}

{% block body %}
    <div class="card mt-1 m-4 p-3">
        <a class="mt-1 mb-2 " href="{{ path('app_admin_style_index') }}"> < Retour</a>
        <h1>Style</h1>

        <table class="table table-dark">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ style.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ style.nom }}</td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex mt-3">
            <a class="btn btn-primary" href="{{ path('app_admin_style_edit', {'id': style.id}) }}">Modifier</a>
            {{ include('admin/style/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
